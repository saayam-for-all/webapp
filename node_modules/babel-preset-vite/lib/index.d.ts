import type babelCore from '@babel/core';
export interface VitePresetOptions {
    env?: boolean;
    glob?: boolean;
    hot?: boolean;
}
declare function vitePreset(_: typeof babelCore, opts: VitePresetOptions): {
    plugins: babelCore.PluginItem[];
};
export default vitePreset;
