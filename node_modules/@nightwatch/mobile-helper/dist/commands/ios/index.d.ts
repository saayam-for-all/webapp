import { Options, SetupConfigs, IosSetupResult } from './interfaces';
export declare class IosSetup {
    options: Options;
    cwd: string;
    platform: 'windows' | 'mac' | 'linux';
    constructor(options: Options, cwd?: string);
    run(): Promise<IosSetupResult | boolean>;
    getConfigFromOptions(options: {
        [key: string]: string | string[] | boolean;
    }): SetupConfigs;
    getSetupConfigs(options: Options): Promise<import("inquirer").Answers>;
    verifySetup(setupConfigs: SetupConfigs): string[];
    setupIos(setupConfigs: SetupConfigs, missingRequirements: string[]): Promise<true | IosSetupResult>;
    showHelp(unknownOptions: string[]): void;
    getAllAvailableOptions: () => string[];
}
//# sourceMappingURL=index.d.ts.map