import { Options, Platform } from '../interfaces';
export declare class AndroidSubcommand {
    sdkRoot: string;
    options: Options;
    subcommand: string;
    rootDir: string;
    platform: Platform;
    androidHomeInGlobalEnv: boolean;
    constructor(subcommand: string, options: Options, rootDir?: string);
    run(): Promise<boolean>;
    loadEnvFromDotEnv(): void;
    executeSubcommand(): Promise<boolean>;
}
//# sourceMappingURL=index.d.ts.map