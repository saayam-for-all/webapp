import { useTranslation } from "react-i18next";

const Skills = ({ setHasUnsavedChanges }) => {
  const { t } = useTranslation();
  return <div className="flex flex-col border p-6 rounded-lg w-full"></div>;
};

export default Skills;
